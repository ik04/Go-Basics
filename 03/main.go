package main

import (
	"fmt"
	"strings"
	"sort"
)

func main()  {
	greeting := "hello friends"
	// fmt.Println(strings.Index(greeting,"hello"))
	arr := (strings.Split(greeting," "))
	fmt.Println(arr[0])
	arr := {1,2,4,5,6}
	sort.arr
}